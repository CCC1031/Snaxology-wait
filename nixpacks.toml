[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["corepack enable && corepack prepare pnpm@latest --activate && pnpm install"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "npx serve dist/public -l tcp://0.0.0.0:$PORT"
